<UserControl
    x:Class="CustomerManagerApp.Wpf.Controls.ListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CustomerManagerApp.Wpf.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel HorizontalAlignment="Center" >

        <StackPanel Margin="10">
            <Button HorizontalAlignment="Stretch" 
                    FontWeight="Bold"
                    Content="Refresh" />

            <Button Margin="0 3 0 0" 
                    HorizontalAlignment="Stretch" 
                    FontWeight="Bold" 
                    Content="New Customer">
            </Button>
            <TextBox Margin="0 3 0 0" 
                     Text="{Binding FilterValue, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                
            </TextBox>
        </StackPanel>

        <ListView MinWidth="200" Margin="10" 
                  ItemsSource="{Binding Customers, Mode=OneWay}"
                  SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}"
                  DisplayMemberPath="DisplayName"
                  />


    </StackPanel>
</UserControl>
