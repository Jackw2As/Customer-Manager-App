<UserControl
    x:Class="CustomerManagerApp.WinUI.Controls.ListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CustomerManagerApp.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel  Background="Beige" HorizontalAlignment="Center">
        <TextBlock HorizontalAlignment="Center" Text="List View" FontWeight="Bold" FontSize="18" Margin="0 5 0 5" />

        <StackPanel>
            <Button HorizontalAlignment="Stretch" FontWeight="Bold" Click="{x:Bind ViewModel.Load}" >Refresh</Button>
            <Button HorizontalAlignment="Stretch" FontWeight="Bold" Click="{x:Bind ViewModel.AddCustomer}" >New Customer</Button>
        </StackPanel>

        <ListView MinWidth="200" Margin="10" 
                  ItemsSource="{x:Bind ViewModel.Customers, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.SelectedCustomer, Mode=TwoWay}"
                  DisplayMemberPath="DisplayName"
                  />


    </StackPanel>
</UserControl>
